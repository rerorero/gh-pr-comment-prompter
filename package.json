{
  "name": "gh-pr-comment-prompter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --watch",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/chrome": "^0.0.304",
    "typescript": "^5.7.3"
  }
}
